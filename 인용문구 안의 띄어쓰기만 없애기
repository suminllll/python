인용문구 안의 띄어쓰기만 없애기

f = 한국어의 띄어쓰기는 "모든 낱말은 띄어쓴다"라는 대원칙을 갖고 있기 때문에 일견 그 규칙은 매우 쉬워보인다. 
    "경우에 따라서는 붙여쓸 수 도 있다"
f_new = ""
k = False
for i in f:       # f안의 글자가 i로 한글자씩 넘어감
  if i == "₩"":   #원 기호가 큰따옴표라는 의미 
     k = not k    # True
  if k == True and i == " ": # 인용문구이면서 띄어쓰기면 패스
    pass
  else:
    f_new += i #인용문구가 아닌 글에만 띄어쓰기가 유지
print(f_new)   
